Glad it helped. Four things I noticed while working on that test.

The same timezone assumption is in two other test files, so they will break the same way on a different machine. The suite also has no fixed clock, so any date-dependent test stays fragile until we set one.

The test setup builds the database URL by joining strings together. A password containing an "@" will break it.

The suite takes about 4 minutes, and roughly 3 of those are one setup step that rebuilds the database schema for every test class. Building it once for the whole suite would remove most of that.

I'd start with the two timezone ones, since they are the same fix. Want me to take those now?
