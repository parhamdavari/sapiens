Glad it helped. Four things worth fixing, in the order I'd do them.

The same timezone assumption is in two other test files. They will fail the same way on a machine set to a different timezone. It's the same small fix as today's.

The test suite has no fixed clock, so any date-dependent test stays fragile. That's the real cause behind today's bug. Setting one clock for the whole suite prevents the next version of it.

Test runtime is four minutes, and about three of those come from a single fixture. It rebuilds the database schema for every test class. Doing that once per run should bring the suite close to a minute.

The test config builds the database URL by joining strings together. A password containing an "@" will break it. That one fails at connection time, and the error message won't point at the cause, so it's worth fixing before someone rotates a password.

I can do the timezone one now if you want. It's quick, and it stops the same failure turning up in the other two files.
