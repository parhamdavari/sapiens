No. It stopped partway, and staging is half-migrated right now.

The first two steps went through and are still in place. The third one timed out after 30 seconds. It was adding a column to the webhook deliveries table, and a long-running analytics query had that table locked.

Re-running the whole file will fail, because the parts that already succeeded can't be created a second time. The safe fix is to run only the third step, once the analytics query is done. That query starts at 02:00 every night and finishes by 02:40.

I should have told you when it failed, instead of leaving it until you asked.

Want me to run that last step tonight after 02:40?
