My best guess is the scheduled reports job. It starts at 14:00 every Tuesday and runs a monthly rollup. That matches your spike window.

I can't confirm it, though. What supports it: the database spends more time waiting on locks during the window, meaning requests queue behind each other. App CPU and memory stay flat, so the delay is inside the database, not the app. Only the slowest requests suffer, since the median barely moves. That fits one heavy job holding locks while normal traffic waits. The two Tuesdays with no spike were both public holidays. That also fits a business job, but holidays change traffic too, so that point is weak on its own.

What's missing is the important part. I can't see which queries the job runs, and there is no query-level database data for that window. So the job is a suspect, not a proven cause. Anything else on a weekly business rhythm could produce the same pattern.

The cheapest way to settle it: move next Tuesday's run to a quiet hour and see whether the spike moves with it. If you'd rather not touch the schedule, turn on query-level logging on the database for those two hours instead.

Want me to look at what that job actually queries?
