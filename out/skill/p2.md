The test result depends on the timezone of the machine running it, so it can't pass in both places. The date grouping uses whatever timezone the process is set to. Amsterdam is one hour ahead of UTC in March, so the order at 23:30 on 1 March falls on 2 March there.

One thing doesn't fit, though, and I'd check it before changing any code. If CI really runs in UTC, the first day there should be 1 March, which is exactly what the test expects. The failure message you got is the one I'd expect from your laptop, not from CI. So either CI isn't actually running in UTC, or the database is applying a timezone of its own.

Check which timezone the database itself uses on CI. That is quick, and it decides which part needs the fix.

The fix in either case is to stop letting the environment decide. Set the timezone explicitly in the query and in the test setup, so the grouping means the same thing everywhere.

One more thing worth changing while you're in there. The test checks the first row for a specific date, and all three seeded orders sit close to midnight. Any timezone shift moves them across the day boundary. Seeding times in the middle of the day would remove that whole class of failure.

Want me to check the database timezone on CI next?
