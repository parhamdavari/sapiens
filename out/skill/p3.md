--- MESSAGE 1 ---

One thing to settle before I delete anything.

Four of the five old helpers are safe to remove. Nothing in the repo references them and they were last changed 14 months ago:

- `migrate_v1_to_v2.py`
- `backfill_legacy_ids.py`
- `dump_old_schema.sh`
- `check_v1_consistency.py`

The fifth one is different. `fix_orphaned_rows.py` is not called from any code, but `docs/runbooks/incident-2025-11.md` names it as the recovery step for an incident that can happen again. If I delete it, that runbook points at a file that no longer exists.

My plan is to delete the four and keep `fix_orphaned_rows.py`. The deletions are permanent in the working tree, though the files stay in git history and can be restored.

Say go and I will run it. If you want all five gone, tell me, and I will update the runbook in the same change.

--- MESSAGE 2 ---

Done. The folder is down to 10 files.

I deleted the four helpers and kept `fix_orphaned_rows.py` for the runbook. Everything left is still used by CI or by `package.json`, so nothing that runs today is affected.

On the tidying: four scripts had no header comment and now carry a one-line description of what the script does. Two were missing the executable bit and now have it. I made no other edits.

One open item. That runbook is the only reason `fix_orphaned_rows.py` is still there. If that kind of incident is behind us, tell me and I will remove the script and fix the runbook together.
