PR #142 makes failed webhook deliveries retry instead of getting dropped. Today a failed send is logged and thrown away. The change puts it in a queue and retries up to 5 times, with a growing wait between attempts. The queue is stored in Redis, so pending retries survive a restart. It is about 400 lines added across 9 files, plus a new database table and 14 new tests.

Two questions are still open, and that is probably where your review time should go. The platform team asked whether this should use the existing cache cluster or get its own, and nobody has answered. The team also has not decided what happens after the fifth failed attempt: page whoever is on call, or give up with no alert. One teammate has already approved it.

Want me to go through the retry logic in detail before tomorrow morning?
